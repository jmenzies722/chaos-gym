# python
.venv/
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
*.egg-info/

# go
*.test
/app/app

# terraform
# .terraform.lock.hcl is intentionally NOT ignored — it pins provider versions
# so a `terraform init` months from now resolves the same AWS provider that was
# actually tested here.
.terraform/
*.tfstate
*.tfstate.backup
terraform.tfvars

CLAUDE.local.md
